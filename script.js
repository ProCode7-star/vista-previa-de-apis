// Esta función se ejecuta automáticamente cuando Google aprueba el reCAPTCHA
function alVerificar(token) {
    console.log("¡Verificación exitosa! Token:", token);
    
    // 🚀 Aquí pones directamente lo que quieres que pase.
    // Por ejemplo, puedes llamar a la función que muestra tus APIs:
    // mostrarMisApis();
}
